// src/pages/BakerSettings.jsx
import Settings from "./Settings";

// Reuse the Settings page for bakers (wrapper kept for routing consistency).
export default function BakerSettings() {
  return <Settings />;
}
